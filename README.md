# CLI Calculator
A CLI Calculator built using C.

## v1 features
- Addition, subtraction, multiplication, division operations
- handles division by 0 correctly
- manages invalid input and no input

## v1.1 features
- Improved program design: the functions are now in separate libraries
- Simplified the input handling functions
